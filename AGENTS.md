# Public Portfolio Development

Work specification-first through DBSCTR. Read the matching files under
`docs/specs/` before implementation.

- Write failing pytest evidence before behavior code.
- Use Ruff for Python linting and formatting.
- Use strict Marimo checks for every notebook.
- Use only synthetic or explicitly redistributable public data.
- Never copy employer, client, DataCamp assessment, SaaS Pegasus, or premium
  template source, prompts, exact schemas, taxonomies, data, screenshots,
  metrics, outputs, or Git history.
- Independently implemented synthetic generalizations may use a prior project
  idea only when provenance discloses the ancestry and no restricted artifact
  crosses the clean-room boundary.
- Never commit credentials, private paths, unapproved personal documents,
  third-party PII, or user-uploaded data. Owner-approved professional
  credential images are allowed only with public-purpose provenance and
  metadata review.
- GitHub source and its README are the portfolio front door. Molab `/wasm`
  links are on-demand derived runtimes. Do not add a separate Pages site.
- Integrate public `main` only through a reviewed pull request.
